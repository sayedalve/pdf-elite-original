# Example Docker Compose for Unified pdf-elite Container
# MODE=FRONTEND: Frontend only, connects to separate backend

services:
  pdf-elite-backend:
    container_name: pdf-elite-Backend
    build:
      context: ../..
      dockerfile: docker/Dockerfile.unified
    ports:
      - "8081:8080"
    volumes:
      - ./stirling/data:/usr/share/tessdata:rw
      - ./stirling/config:/configs:rw
      - ./stirling/logs:/logs:rw
      - ./stirling/customFiles:/customFiles:rw
      - ./stirling/pipeline:/pipeline:rw
    environment:
      MODE: BACKEND
      DISABLE_ADDITIONAL_FEATURES: "false"
      DOCKER_ENABLE_SECURITY: "false"
      PUID: 1000
      PGID: 1000
      UMASK: "022"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080$${SYSTEM_ROOTURIPATH:-''}/api/v1/info/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 4G

  pdf-elite-frontend:
    container_name: pdf-elite-Frontend
    build:
      context: ../..
      dockerfile: docker/Dockerfile.unified
    ports:
      - "8080:8080"
    environment:
      MODE: FRONTEND

      # Point to the backend service
      VITE_API_BASE_URL: http://pdf-elite-backend:8080

      # Minimal config needed for frontend
      PUID: 1000
      PGID: 1000
    depends_on:
      - pdf-elite-backend
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
