/**
 * Desktop-only footer shown at the bottom of the tool list.
 * In PDF Elite, all tools are offline and there is no sign-in required.
 */
export function ToolPickerFooterExtensions() {
  return null;
}
